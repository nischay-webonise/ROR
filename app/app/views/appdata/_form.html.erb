<%= form_for(@appdatum) do |f| %>
  <% if @appdatum.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@appdatum.errors.count, "error") %> prohibited this appdatum from being saved:</h2>

      <ul>
      <% @appdatum.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <p>
  <%= f.label :name %>
  <%= f.text_area :name %>
  </p>
  <p>
  <%= f.label :description %>
  <%= f.text_area :description %>
  </p>
    <p>
  <%= f.label :phone_number %>
  <%= f.text_area :phone_number %>
  </p>
 <p>
  <%= f.label :address %>
  <%= f.text_area :address %>
  </p>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
